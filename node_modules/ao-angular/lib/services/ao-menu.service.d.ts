import { MenuController, Platform } from '@ionic/angular';
export declare class AOMenuService {
    platform: Platform;
    controller: MenuController;
    private _stacked;
    constructor(platform: Platform, controller: MenuController);
    disable(): Promise<HTMLIonMenuElement>;
    enable(): Promise<HTMLIonMenuElement>;
    stackable(): boolean;
    stacked(): any;
    stackToggle(): void;
}
