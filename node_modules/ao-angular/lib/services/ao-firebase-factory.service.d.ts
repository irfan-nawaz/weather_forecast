import { NgZone } from '@angular/core';
import { FirebaseApp, FirebaseAppConfig } from '@angular/fire';
import { AOFirebaseConnectionService } from './ao-firebase-connection.service';
export declare class AOFirebaseFactoryService {
    private _app;
    private _zone;
    private _environment;
    private connections;
    constructor(_app: FirebaseApp, _zone: NgZone, _environment: any);
    conn(name?: string): AOFirebaseConnectionService;
    makeHash(configs: FirebaseAppConfig): string;
    factory(name: string, configs?: FirebaseAppConfig, app?: FirebaseApp): Promise<boolean>;
    destroy(only?: string[], except?: string[]): Promise<boolean>;
}
