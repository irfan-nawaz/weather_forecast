import { ElementRef, NgZone, OnDestroy, OnInit, TemplateRef, ViewContainerRef } from '@angular/core';
export declare class AOIsGuestDirective implements OnInit, OnDestroy {
    private _element;
    private _templateRef;
    private _viewContainer;
    private _zone;
    private _subscriptions;
    aoIsGuest: any;
    constructor(_element: ElementRef, _templateRef: TemplateRef<any>, _viewContainer: ViewContainerRef, _zone: NgZone);
    ngOnInit(): void;
    ngOnDestroy(): void;
}
